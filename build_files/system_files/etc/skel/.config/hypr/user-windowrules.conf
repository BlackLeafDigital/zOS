# =============================================================================
# User Window Rules
# Add your custom window rules here. These persist across OS updates.
# See system defaults: /usr/share/zos/hypr/windowrules.conf
# =============================================================================

# Examples:
# windowrule = float, class:^(firefox)$, title:^(Library)$
# windowrule = workspace 2, class:^(discord)$
