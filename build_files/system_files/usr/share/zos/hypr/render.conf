# =============================================================================
# Render tuning — SYSTEM MANAGED
# =============================================================================

render {
    # Direct scanout for fullscreen games (auto mode = only when content type
    # is 'game'; windowed use unaffected). Reduces input latency on a single
    # fullscreen app; safe in normal desktop use.
    direct_scanout = 2
}
